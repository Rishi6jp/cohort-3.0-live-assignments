// create a funciton to take two inputs with map
// an array and a transformation callback/fn
// and transforms the array into a new one using the transformation fn

arr = []

function inp() {
    for(let i = 0; i<2; i++ ){

    }
}

const arr = arr.map()

